{
    total: {
      all: sumAll,
      all_available: getDevAvanonit + getItAva,
      all_onboard: getDevOnnonit + getItOn,
      center: getFillAllCenter
        ? getFillAllCenter
        : getFillCenter
        ? getFillCenter
        : defaultCenter,
      matchjob: projectCount ? projectCount : defaultMatchjob,
    },
    dashboard: {
      total_persons: sumDev + getIt || 0,
      // developer: sumDev || 0,
      developer: sumOut || 0,
      dev_onboard: !statusAvailable
        ? getDevOnnonit
        : !statusOnboard
        ? 0
        : getDevOnnonit,
      dev_available: statusAvailable
        ? getDevAvanonit
        : statusOnboard
        ? 0
        : getDevAvanonit,
      // it_outsource: getIt || 0,
      it_outsource: getItAva + getItOn || 0,
      it_onboard: !statusAvailable ? getItOn : !statusOnboard ? 0 : getItOn,
      it_available: statusAvailable ? getItAva : statusOnboard ? 0 : getItAva,
    },
  }